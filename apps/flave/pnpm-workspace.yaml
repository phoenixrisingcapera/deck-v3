packages:
  - 'packages/*'
  - 'apps/*'

# vite depends on esbuild, whose postinstall places the platform binary.
# Without this, install "succeeds" and every vite invocation then fails.
allowBuilds:
  esbuild: true
